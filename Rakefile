# Add your own task in files placed in lib/task ending in .rake,
# for example lib/task/capistrano.rake, and they will automatically be available to Rake.

require_relative 'config/application'

Rails.application.load_tasks
